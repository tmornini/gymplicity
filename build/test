#!/bin/bash
set -euo pipefail

SCHEME="Gymplicity"
DESTINATION="platform=iOS Simulator,name=${1:-iPhone 17 Pro}"

xcodebuild test \
  -scheme "$SCHEME" \
  -destination "$DESTINATION" \
  -configuration Debug \
  2>&1
